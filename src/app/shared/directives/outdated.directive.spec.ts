import { OutdatedDirective } from './outdated.directive';

describe('OutdatedDirective', () => {
  it('should create an instance', () => {
    const directive = new OutdatedDirective();
    expect(directive).toBeTruthy();
  });
});
